<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Pantry</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>_____'s Catalog</h1>
    <h2>Your beats:</h2>
    <ul>
        <% catalog.forEach((beat) => { %>
            <li>
                <a href="/users/<%= user._id %>/beats/<%= beat._id %>/edit"><%= beat.title %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>